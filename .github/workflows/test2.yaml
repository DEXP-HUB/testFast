name: Test1
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        - name: Echo GitHub
          run: echo ${{ toJson(github) }}
        
        



