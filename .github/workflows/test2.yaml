name: Test2
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

jobs:
  build:
    if: ${{ github.actor == 'DEXP-HU' }}
    runs-on: ubuntu-latest

    steps:
        - name: Echo GitHub
          run: echo "${{ toJson(github) }}"
        
        



